# include <stdio.h>
int main() {
    int a;
    printf("Enter an integer \n");
    scanf("%d",&a);
    // one line me if else
    (a > 5) ? printf("integer is greater than 5"): printf("integer is less than 5");
     return 0;
}