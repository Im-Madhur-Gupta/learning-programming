#include <stdio.h>
int main()
{
    int a=5,b=5;
    printf("a++ is %d \n",a++); // a++ matlab pehle print kare fir increment kare
    printf("++b is %d \n",++b); // same to same for --
    // ++b pehle increment kare phir print kare
    return 0;
}