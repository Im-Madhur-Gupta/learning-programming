#include <stdio.h>
int main()
{
// memory reserved using malloc / calloc isn't automatically deallocated
// so, to deallocate / free the reserved memory we have free() function
// syntax - free(pointer);
return 0;
}