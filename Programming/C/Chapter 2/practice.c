#include <stdio.h>
int main()
{
    char t ='@'; // char datatype can only store one alphabet, number, specials , any thing but sirf ek .
    printf("%c" ,t);

    // c language me by default decimal values double datatype me ati he .
    return 0;
}